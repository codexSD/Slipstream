package com.slipstream.meridian

import androidx.compose.material3.Typography
import androidx.compose.runtime.Composable

/**
 * STUB for Task 3 so `MeridianTheme.kt` compiles. Task 4 replaces this with
 * Meridian's full type scale, where every numeric text style carries
 * `fontFeatureSettings = "tnum"` per the Global Constraints.
 */
@Composable
internal fun meridianTypography(): Typography = Typography()
